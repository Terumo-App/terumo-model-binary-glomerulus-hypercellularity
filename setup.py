from setuptools import find_packages, setup

setup(
    name='terumo_model_binary_glomerulus_hypercellularity',
    version='0.1',
    description='My module',
    packages=find_packages(where='src'),
    package_dir={'': 'src'},
)
